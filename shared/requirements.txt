# === Live Caos — dependencias de execucao ===
TikTokLive>=6.0.0
websockets>=12.0
python-dotenv>=1.0.0

# Teclado (Windows): pydirectinput injeta scancodes que jogos Source leem
pydirectinput>=1.0.4; sys_platform == "win32"
pyautogui>=0.9.54

# Deteccao da janela ativa do jogo
PyGetWindow>=0.0.9
